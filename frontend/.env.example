# Local frontend → Payload (pnpm dev)
NEXT_PUBLIC_PAYLOAD_API_URL=http://localhost:4001

# On Vercel, leave NEXT_PUBLIC_PAYLOAD_API_URL unset.
# Server fetches use the PAYLOAD_API_URL service binding (injected automatically).
# Public /api/* is rewritten to the backend service.
